
ATTRIBUTE = "attribute"
UNKNOWN = "unknown"

## Key words
ENUM = "enum"
PROTOCOL = "protocol"
STRUCT = "struct"
CLASS = "class"
FUNC = "func"
VAR = "var"
LET = "let"
IF = "if"
ELSE = "else"
EXTENSION = "extension"
GENERICS = "generics"

swiftKeyWords = [
    ENUM,
    PROTOCOL,
    STRUCT,
    CLASS,
    EXTENSION,
    FUNC,
    VAR,
    LET,
    IF,
    ELSE
]