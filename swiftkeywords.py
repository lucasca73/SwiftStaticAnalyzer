
ATTRIBUTE = "attribute"

## Key words
ENUM = "enum"
PROTOCOL = "protocol"
STRUCT = "struct"
CLASS = "class"
FUNC = "func"
VAR = "var"
LET = "let"
IF = "if"
ELSE = "else"

swiftKeyWords = [
    ENUM,
    PROTOCOL,
    STRUCT,
    CLASS,
    FUNC,
    VAR,
    LET,
    IF,
    ELSE
]